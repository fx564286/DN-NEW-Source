name: Build Cineseat Alpha119 APK
on:
  push:
    branches: [cineseat-alpha28-build]
    paths:
      - '.github/workflows/cineseat-alpha118-source.yml'
      - 'alpha118_src/**'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.6'
          channel: stable
          cache: true
      - name: Create Android project
        shell: bash
        run: |
          set -euo pipefail
          flutter create --platforms=android --org com.fx564286 --project-name cinema_seat_alert build_app
          rm -rf build_app/lib build_app/test
          mkdir -p build_app/lib
          cp alpha118_src/lib/*.dart build_app/lib/
          python3 alpha118_src/apply_alpha119_megabox.py build_app
          cat > build_app/pubspec.yaml <<'YAML'
          name: cinema_seat_alert
          description: Cineseat alpha119 CGV Megabox exact seats
          publish_to: none
          version: 0.2.1-alpha.119+119
          environment:
            sdk: '>=3.6.0 <4.0.0'
          dependencies:
            flutter:
              sdk: flutter
            crypto: ^3.0.6
            geolocator: ^14.0.2
            http: ^1.5.0
            shared_preferences: ^2.5.3
          dev_dependencies:
            flutter_test:
              sdk: flutter
            flutter_lints: ^6.0.0
          flutter:
            uses-material-design: true
          YAML
          python3 - <<'PY'
          from pathlib import Path
          p=Path('build_app/android/app/src/main/AndroidManifest.xml')
          s=p.read_text()
          marker='<manifest xmlns:android="http://schemas.android.com/apk/res/android">'
          perms='''\n    <uses-permission android:name="android.permission.INTERNET" />\n    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />\n    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />\n    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />'''
          s=s.replace(marker, marker+perms, 1)
          p.write_text(s)
          PY
          grep -q 'CinemaChain.megabox when showtime.id.isNotEmpty' build_app/lib/services.dart
          grep -q '메가박스 공식 selectSeatList · 실제 좌석 데이터' build_app/lib/services.dart
          grep -q 'theater.chain == CinemaChain.megabox' build_app/lib/wizard.dart
          grep -q 'CGV 공식 searchIfSeatData · 비회원 실제 좌석 데이터' build_app/lib/wizard.dart
      - name: Resolve and analyze
        working-directory: build_app
        run: |
          flutter pub get
          dart format lib
          flutter analyze --no-fatal-warnings --no-fatal-infos
      - name: Build alpha119 APK
        working-directory: build_app
        run: flutter build apk --release --build-name 0.2.1-alpha.119 --build-number 119
      - name: Verify APK
        shell: bash
        run: |
          set -euo pipefail
          APK=build_app/build/app/outputs/flutter-apk/app-release.apk
          test -s "$APK"
          unzip -t "$APK" >/tmp/unzip.txt
          tail -n 2 /tmp/unzip.txt
          BT="$(find "$ANDROID_HOME/build-tools" -mindepth 1 -maxdepth 1 -type d | sort -V | tail -1)"
          "$BT/apksigner" verify --verbose --print-certs "$APK"
          mkdir -p dist
          cp "$APK" dist/Cineseat_0.2.1-alpha119_CGV_MEGABOX_EXACT.apk
          sha256sum dist/Cineseat_0.2.1-alpha119_CGV_MEGABOX_EXACT.apk | tee dist/Cineseat_0.2.1-alpha119_CGV_MEGABOX_EXACT.sha256
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Cineseat_0.2.1-alpha119_CGV_MEGABOX_EXACT
          path: dist/*
          if-no-files-found: error
          retention-days: 7
